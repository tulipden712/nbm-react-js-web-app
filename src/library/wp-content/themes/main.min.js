! function(e) {
    var n = e(".portfolio-wrapper, .content-area.masonry .posts-loop, .content-area.masonry-fullwidth .posts-loop").isotope({ layoutMode: "masonry", itemSelector: ".hentry", cellsByRow: { columnWidth: ".hentry" } });
    n.imagesLoaded().progress(function() { n.isotope("layout") })
}(jQuery),
function(e) { new WOW({ mobile: !0, callback: function(n) { e(n).addClass("project-visible") } }).init() }(jQuery),
function(e) { 
    //e(".typed-element").typed({ typeSpeed: 75, loop: !0, backDelay: 2e3, stringsElement: e(".typed-strings") })
    //new Typed(e(".typed-element"), { typeSpeed: 75, loop: !0, backDelay: 2e3, stringsElement: e(".typed-strings") }) 
}(jQuery),
function(e) { e(".header-search a").on("click", function(n) { n.preventDefault(), e(".header-search-form").slideToggle() }) }(jQuery),
function(e) { e(".social-navigation li a").attr("target", "_blank") }(jQuery),
function(e) {
    var n = "desktop";
    e(window).on("load resize", function() {
        var t = "desktop";
        if (matchMedia("only screen and (max-width: 991px)").matches && (t = "mobile"), t !== n)
            if (n = t, "mobile" === t) {
                e("#site-navigation").attr("id", "mainnav-mobi").hide();
                var i = e("#mainnav-mobi").find("li:has(ul)");
                i.children("ul").hide(), i.children("a").after('<span class="btn-submenu"></span>'), e(".btn-menu .icon-menu").removeClass("active")
            } else e("#mainnav-mobi").attr("id", "site-navigation").removeAttr("style").find(".submenu").removeAttr("style"), e(".btn-submenu").remove()
    }), e(".btn-menu .icon-menu, .btn-close-menu").on("click", function() { e("#mainnav-mobi").slideToggle(300), e(this).toggleClass("active") }), e(document).on("click", "#mainnav-mobi li .btn-submenu", function(n) { e(this).toggleClass("active").next("ul").slideToggle(300), n.stopImmediatePropagation() })
}(jQuery), jQuery(function(e) { e(window).on("ready load", function() { /* e("body").fitVids() */ }) });

jQuery(function($) {
    $.fn.extend({
        showLoading: function () {
            $('.preloader').css('opacity', 1);
            $('.preloader').show();
        },
        hideLoading: function () {
            $('.preloader').css('opacity', 0);
            $('.preloader').hide();
        },
        isOverflow: function() {
            if ($(window).width() < 992) return true;
            var fontSize = $("html").css('font-size');
            switch(fontSize) {
                case '14px':
                    return ($(window).height() < 535);
                    break;
                case '16px':
                    return ($(window).height() < 552);
                    break;
                case '17px':
                    return ($(window).height() < 600);
                    break;
                case '18px':
                    return ($(window).height() < 627);
                    break;
                case '22px':
                    return ($(window).height() < 734);
                    break;
                case '27px':
                    return ($(window).height() < 882);
                    break;
                default:
                    break;
            }
        },
        blurHeader: function() {
            const distanceY = window.pageYOffset || document.documentElement.scrollTop,
            shrinkOn = 10,
            $headerEl = $('#masthead');
            
            if (distanceY > shrinkOn) {
                $headerEl.addClass("site-header--blur");
            } else {
                $headerEl.removeClass("site-header--blur");
            }
        }
    });
    var removePreloader = function() {
        $('.preloader').css('opacity', 0);
        $('.preloader').hide();
    }
    removePreloader();

    // contact us page
    $('.contact-us-single-page-content__detail__contact-head').click(function() {
        if ($(this).children('.contact-info-detail').hasClass('active')) return;
        $('.contact-info-detail.active').slideUp().removeClass('active');
        $(this).children('.contact-info-detail').slideDown().addClass('active');
    });

});